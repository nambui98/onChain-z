---
import MainHead from '../components/Site/MainHead.astro';
import Footer from '../components/Footer/index.jsx';
import Nav from '../components/Nav/index.jsx';
import PortfolioPreview from '../components/PortfolioPreview/index.jsx';

const products = (await Astro.glob('./products/**/*.md'))
	.filter(({ frontmatter }) => !!frontmatter.publishDate)
	.sort(
		(a, b) =>
			new Date(b.frontmatter.publishDate).valueOf() - new Date(a.frontmatter.publishDate).valueOf()
	);
---

<html lang="en">

<head>
	<MainHead title="All Products | Gurucore" description="Learn about Gurucore's most recent products" />
	<style lang="scss">
		.grid {
			display: grid;
			grid-gap: 3rem;
		}
	</style>
</head>

<body>
	<Nav />
	<div class="wrapper">
		<h1 class="title mt4 mb4">All Products</h1>
		<div class="grid">
			{products.map((project) =>
			<PortfolioPreview project={project} />)}
		</div>
	</div>
	<Footer />
</body>

</html>